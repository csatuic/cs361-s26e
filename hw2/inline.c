#include<string.h>
int main() {
    char *greeting = "Hello, World!\n";
    long len = strlen(greeting); // having the length in a temporary variable makes things easier...
    
    // inline assembly here, make sure you print the greeting, 
    // not a hard-coded value inside the assembly block
}